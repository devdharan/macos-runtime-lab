# macOS Runtime Lab

A generic GitHub Actions harness for reproducible macOS runtime experiments.
Experiment inputs and outputs are encrypted at rest; workflow logs expose only
an exit status and artifact checksum.
